<!DOCTYPE html>
<html lang="es">
<head>
    <%- include("partials/head") %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <!-- ---------------------------- HEADER ---------------------------- -->
   <header>
    <%- include("partials/header") %>
   </header>
    <main>
    
        <div class="container">
            <div class="logo">
                <h2 class="products-title title">
                    Detalle del producto: <%= product.name %>
                </h2>
            </div>
    
        <div class="left">
         <img class="image-product"src="/images/products/<%= product.image %>" alt="product image" class="product-detail-img" />
         <p class="product-detail-description">
            <%= product.description %>
        </p>
        </div>
        <div class="rigth">

      
            <div class="col-12 col-lg-4">
                <article class="product-detail-info">
                    <h2 class="products-title">
                        <%= product.name %>
                    </h2>

                    <% if (product.discount) { %>
                    <p class="product-detail-price small">
                        <span class="descuento-2  tach"><%= product.price %></span>
                        /
                        <b class="descuento-2"><%= product.discount %>% OFF</b>
                    </p>
                    <p class="product-detail-price">
                        <%= product.priceWithDiscount%>
                    </p>
                    <% } else { %>
                    <p >
                        <%= product.priceWithDiscount %>
                    </p>
                    <% } %>

                    <ul class="actions-list">
                        <li>
                            <i class="fas fa-credit-card  emoticon"></i>
                            <p class="cuotas">Pagá en 12 cuotas sin interes</p>
                        </li>
                        <li>
                            <i class="fas fa-store emoticon"></i>
                            <p class="cuotas">Retiro gratis en locales del vendedor</p>
                        </li>
                    </ul>

                    <a href="#" class="buy-now-button">COMPRAR AHORA</a>
                    <hr />

                    <a href="/products/edit/<%= product.id %>" class="action-button-edit">EDITAR PRODUCTO</a>
                    <form action="/products/<%= product.id %>?_method=delete" method="POST"
                        style="display: inline-flex">
                        <button type="submit" class="action-button-delete">
                            ELIMINAR
                        </button>
                    </form>
                </article>
            </div>
        </div>
    </div>
</div>
</main>
     
    </main>
    <footer>
        <%- include("partials/footer") %>
    </footer>